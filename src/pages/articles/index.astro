---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";

const items = (await getCollection("articles"))
  .sort((a, b) => a.data.title.localeCompare(b.data.title));
---
<BaseLayout title="Articles" description={`${items.length} rhubarb articles preserved from www.rhubarbinfo.com`}>
  <h1>Articles ({items.length})</h1>
  <ul class="listing">
    {items.map((r) => (
      <li>
        <a href={`${import.meta.env.BASE_URL}articles/${r.id}/`}>{r.data.title}</a>
        {r.data.description && <span class="desc">{r.data.description}</span>}
      </li>
    ))}
  </ul>
</BaseLayout>
